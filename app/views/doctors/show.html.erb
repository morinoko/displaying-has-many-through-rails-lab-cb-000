<h1><%= @doctor.name %></h1>
<h2>Department: <%= @doctor.department %></h2>

<ul>
  <% @doctor.appointments.each do |appointment| %>
    <li><%= "#{appointment.date} at #{appointment.time} " at  %> with <%= link_to appointment.patient_name, patient_path(appointment.patient) %></li>
  <% end %>
</ul>
